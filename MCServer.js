"use strict";
class MCServer {
    constructor(socket) {
        this.socket = name;
        
        this.players = [];
    }
    
    getPlayers() {
        return this.players;
    }
    
    getInfo(key, options) {
        s
        return new Promise();
    }
}

module.exports = MCServer;